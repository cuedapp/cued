import { BarChart3, Clock3, Film, Star, Tv, UsersRound } from "lucide-react";
import { notFound } from "next/navigation";
import { getLocale, getTranslations } from "next-intl/server";
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from "@/components/ui/card";
import { getCurrentUser } from "@/server/auth/session";
import { activityService } from "@/server/application/services";
import { formatEstimatedWatchTime } from "@/lib/activity-time";
import { formatRelativeDate } from "@/lib/date-time";

export default async function StatisticsPage() {
  const user = await getCurrentUser();
  if (!user || user.role !== "admin") notFound();
  const [t, activityT, locale, summaries, server] = await Promise.all([getTranslations("Statistics"), getTranslations("Activity"), getLocale(), activityService.getAdminUserSummaries(), activityService.getServerStatistics()]);
  const serverWatchTime = formatEstimatedWatchTime(server.estimatedWatchSeconds);
  return <div className="space-y-8"><header className="max-w-2xl"><p className="text-xs font-bold uppercase tracking-[0.18em] text-primary">{t("eyebrow")}</p><h1 className="mt-3 font-display text-5xl font-semibold tracking-tighter">{t("title")}</h1><p className="mt-4 leading-7 text-muted-foreground">{t("description")}</p></header><section className="space-y-4"><div><h2 className="font-display text-3xl font-semibold tracking-tight">{t("serverTitle")}</h2><p className="mt-1 text-sm text-muted-foreground">{t("serverDescription")}</p></div><div className="grid gap-5 sm:grid-cols-2 xl:grid-cols-4"><StatCard icon={<Film className="size-4" />} label={t("movies")} value={server.movies} /><StatCard icon={<Tv className="size-4" />} label={t("series")} value={server.series} /><StatCard icon={<UsersRound className="size-4" />} label={t("users")} value={server.users} /><StatCard icon={<Clock3 className="size-4" />} label={t("watchTime")} value={activityT(serverWatchTime.unit, { hours: serverWatchTime.value, days: serverWatchTime.value, weeks: serverWatchTime.value })} /></div><div className="grid gap-5 xl:grid-cols-3"><TitleList title={t("mostWatched")} items={server.mostWatched} empty={t("noTitleData")} value={(item) => t("viewers", { count: item.watchers ?? 0 })} /><TitleList title={t("highestRated")} items={server.highestRated} empty={t("noTitleData")} value={(item) => t("rating", { rating: item.averageRating ?? 0, count: item.ratings ?? 0 })} /><TitleList title={t("lowestRated")} items={server.lowestRated} empty={t("noTitleData")} value={(item) => t("rating", { rating: item.averageRating ?? 0, count: item.ratings ?? 0 })} /></div></section><section className="space-y-4"><div><h2 className="font-display text-3xl font-semibold tracking-tight">{t("usersTitle")}</h2><p className="mt-1 text-sm text-muted-foreground">{t("usersDescription")}</p></div><div className="grid gap-5 xl:grid-cols-2">{summaries.map((summary) => { const watchTime = formatEstimatedWatchTime(summary.estimatedWatchSeconds); return <Card key={summary.id}><CardHeader><div className="flex items-center gap-2"><UsersRound className="size-4 text-primary" /><CardTitle>{summary.displayName}</CardTitle></div><CardDescription>{summary.lastPlayedAt ? t("lastActive", { date: formatRelativeDate(summary.lastPlayedAt, new Date(), locale, user.dateFormat) }) : t("notActive")}</CardDescription></CardHeader><CardContent><dl className="grid grid-cols-2 gap-4 text-sm"><div><dt className="flex items-center gap-1.5 text-muted-foreground"><Clock3 className="size-3.5" />{t("watchTime")}</dt><dd className="mt-1 font-semibold">{activityT(watchTime.unit, { hours: watchTime.value, days: watchTime.value, weeks: watchTime.value })}</dd></div><div><dt className="flex items-center gap-1.5 text-muted-foreground"><BarChart3 className="size-3.5" />{t("watchedTitles")}</dt><dd className="mt-1 font-semibold">{summary.watchedTitles}</dd></div><div><dt className="flex items-center gap-1.5 text-muted-foreground"><Star className="size-3.5" />{t("ratings")}</dt><dd className="mt-1 font-semibold">{summary.averageRating === null ? t("noRatings") : t("rating", { rating: summary.averageRating, count: summary.ratings })}</dd></div></dl><div className="mt-6 border-t border-border pt-5"><h3 className="text-sm font-semibold">{t("recentTitle")}</h3>{summary.recent.length === 0 ? <p className="mt-2 text-sm text-muted-foreground">{t("recentEmpty")}</p> : <ul className="mt-3 space-y-2">{summary.recent.map((item) => <li key={`${item.kind}:${item.name}:${item.lastPlayedAt.toISOString()}`} className="flex items-center justify-between gap-3 text-sm"><span className="min-w-0 truncate font-medium">{item.name}</span><span className="shrink-0 text-muted-foreground">{formatRelativeDate(item.lastPlayedAt, new Date(), locale, user.dateFormat)}</span></li>)}</ul>}</div></CardContent></Card>; })}</div></section></div>;
}

function StatCard({ icon, label, value }: { icon: React.ReactNode; label: string; value: React.ReactNode }) { return <Card><CardContent className="pt-6"><dt className="flex items-center gap-1.5 text-sm text-muted-foreground">{icon}{label}</dt><dd className="mt-2 font-display text-3xl font-semibold">{value}</dd></CardContent></Card>; }
type TitleStatistic = { name: string; watchers?: number; averageRating?: number; ratings?: number };
function TitleList({ title, items, empty, value }: { title: string; items: TitleStatistic[]; empty: string; value: (item: TitleStatistic) => string }) { return <Card><CardHeader><CardTitle className="text-lg">{title}</CardTitle></CardHeader><CardContent>{items.length === 0 ? <p className="text-sm text-muted-foreground">{empty}</p> : <ol className="space-y-2">{items.map((item, index) => <li key={`${item.name}:${index}`} className="flex justify-between gap-3 text-sm"><span className="min-w-0 truncate font-medium">{item.name}</span><span className="shrink-0 text-muted-foreground">{value(item)}</span></li>)}</ol>}</CardContent></Card>; }

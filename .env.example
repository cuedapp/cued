# Bootstrap infrastructure only. Configure provider integrations inside Cued.
DATABASE_URL=postgresql://cued:cued@127.0.0.1:5433/cued
# Generate with: openssl rand -base64 32
CUED_ENCRYPTION_KEY=
LOG_LEVEL=info

"use client";
import { useActionState, useEffect, useState } from "react";
import { useTranslations } from "next-intl";
import { toast } from "sonner";
import { FormSubmitButton } from "@/components/form-submit-button";
import { Input } from "@/components/ui/input";
import { Label } from "@/components/ui/label";
import { syncM3uEditor, updateM3uEditorConfiguration, type M3uEditorFormState } from "./actions";

type Library = { id: string; name: string; collectionType: string | null };
export function M3uEditorForm({ locale, overview }: { locale: string; overview: { baseUrl: string; username: string; playlistUuid: string; movieDirectory: string; seriesDirectory: string; refreshJellyfin: boolean; hasPassword: boolean; encryptionConfigured: boolean; movieLibraryIds: string[]; seriesLibraryIds: string[]; libraries: Library[]; configured: boolean } }) {
  const t = useTranslations("M3uEditorIntegration"); const [state, action] = useActionState(updateM3uEditorConfiguration, {} as M3uEditorFormState); const [syncState, syncAction] = useActionState(syncM3uEditor, {} as M3uEditorFormState);
  const [baseUrl, setBaseUrl] = useState(overview.baseUrl); const [username, setUsername] = useState(overview.username); const [password, setPassword] = useState(""); const [playlistUuid, setPlaylistUuid] = useState(overview.playlistUuid); const [movieDirectory, setMovieDirectory] = useState(overview.movieDirectory); const [seriesDirectory, setSeriesDirectory] = useState(overview.seriesDirectory); const [movieLibraryIds, setMovieLibraryIds] = useState(overview.movieLibraryIds); const [seriesLibraryIds, setSeriesLibraryIds] = useState(overview.seriesLibraryIds);
  useEffect(() => { const current = state.error ? state : syncState; if (current.error) toast.error(t(`errors.${current.error}`)); if (current.result) toast.success(t(`results.${current.result}`)); }, [state, syncState, t]);
  return <div className="space-y-6"><form action={action} className="space-y-5"><input type="hidden" name="locale" value={locale} /><div className="grid gap-4 sm:grid-cols-2"><div className="space-y-2"><Label htmlFor="m3uBaseUrl">{t("baseUrl")}</Label><Input id="m3uBaseUrl" name="baseUrl" type="url" value={baseUrl} onChange={(event) => setBaseUrl(event.target.value)} required /></div><div className="space-y-2"><Label htmlFor="m3uUsername">{t("username")}</Label><Input id="m3uUsername" name="username" value={username} onChange={(event) => setUsername(event.target.value)} required /></div></div><div className="space-y-2"><Label htmlFor="m3uPassword">{t("password")}</Label><Input id="m3uPassword" name="password" type="password" value={password} onChange={(event) => setPassword(event.target.value)} placeholder={overview.hasPassword ? t("passwordStored") : undefined} disabled={!overview.encryptionConfigured} autoComplete="off" /><p className="text-xs text-muted-foreground">{t("passwordHelp")}</p></div><div className="space-y-2"><Label htmlFor="m3uPlaylistUuid">{t("playlistUuid")}</Label><Input id="m3uPlaylistUuid" name="playlistUuid" value={playlistUuid} onChange={(event) => setPlaylistUuid(event.target.value)} /><p className="text-xs text-muted-foreground">{t("playlistUuidHelp")}</p></div><div className="grid gap-4 sm:grid-cols-2"><div className="space-y-2"><Label htmlFor="m3uMovieDirectory">{t("movieDirectory")}</Label><Input id="m3uMovieDirectory" name="movieDirectory" value={movieDirectory} onChange={(event) => setMovieDirectory(event.target.value)} required /></div><div className="space-y-2"><Label htmlFor="m3uSeriesDirectory">{t("seriesDirectory")}</Label><Input id="m3uSeriesDirectory" name="seriesDirectory" value={seriesDirectory} onChange={(event) => setSeriesDirectory(event.target.value)} required /></div></div><p className="text-xs text-muted-foreground">{t("directoryHelp")}</p><label className="flex cursor-pointer items-start gap-3 rounded-xl border border-border p-4"><input type="checkbox" name="refreshJellyfin" defaultChecked={overview.refreshJellyfin} className="mt-1 size-4 cursor-pointer accent-primary" /><span><span className="block text-sm font-medium">{t("refreshJellyfin")}</span><span className="text-xs leading-5 text-muted-foreground">{t("refreshJellyfinHelp")}</span></span></label><LibraryAccess title={t("movieLibraries")} help={t("movieLibrariesHelp")} name="movieLibraryIds" libraries={overview.libraries} selected={movieLibraryIds} onChange={setMovieLibraryIds} /><LibraryAccess title={t("seriesLibraries")} help={t("seriesLibrariesHelp")} name="seriesLibraryIds" libraries={overview.libraries} selected={seriesLibraryIds} onChange={setSeriesLibraryIds} /><div className="flex flex-wrap gap-3"><FormSubmitButton name="intent" value="save" pendingLabel={t("saving")}>{t("save")}</FormSubmitButton><FormSubmitButton name="intent" value="test" variant="outline" pendingLabel={t("testing")}>{t("test")}</FormSubmitButton></div></form>{overview.configured && <form action={syncAction}><input type="hidden" name="locale" value={locale} /><FormSubmitButton variant="outline" pendingLabel={t("syncing")}>{t("sync")}</FormSubmitButton></form>}</div>;
}
function LibraryAccess({ title, help, name, libraries, selected, onChange }: { title: string; help: string; name: string; libraries: Library[]; selected: string[]; onChange: (ids: string[]) => void }) { return <fieldset className="space-y-3"><legend className="text-sm font-medium">{title}</legend><p className="text-xs text-muted-foreground">{help}</p><div className="grid gap-2 sm:grid-cols-2">{libraries.map((library) => <label key={library.id} className="flex cursor-pointer items-center gap-3 rounded-xl border border-border p-3 text-sm"><input type="checkbox" name={name} value={library.id} checked={selected.includes(library.id)} onChange={(event) => onChange(event.target.checked ? [...selected, library.id] : selected.filter((id) => id !== library.id))} className="size-4 cursor-pointer accent-primary" />{library.name}</label>)}</div></fieldset>; }
